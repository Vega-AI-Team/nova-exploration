name: Jazzy Build

on:
  push:
  pull_request:
  workflow_dispatch:

concurrency:
  group: jazzy-build-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-24.04
    container:
      image: ros:jazzy-ros-base-noble

    defaults:
      run:
        shell: bash

    env:
      ROS_DISTRO: jazzy

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Install system dependencies
        run: |
          apt-get update
          apt-get install -y qtbase5-dev

      - name: Install package dependencies
        run: |
          rosdep update
          rosdep install \
            --from-paths . plugin/nova_exploration_rviz \
            --ignore-src \
            --rosdistro "$ROS_DISTRO" \
            -r \
            -y

      - name: Build core package
        run: |
          source /opt/ros/$ROS_DISTRO/setup.bash
          colcon build \
            --event-handlers console_direct+ \
            --build-base build-core \
            --install-base install \
            --packages-select nova_exploration

      - name: Build RViz plugin package
        run: |
          source /opt/ros/$ROS_DISTRO/setup.bash
          source install/setup.bash
          colcon build \
            --event-handlers console_direct+ \
            --base-paths . plugin/nova_exploration_rviz \
            --build-base build-all \
            --install-base install \
            --packages-select nova_exploration_rviz \
            --cmake-args -DPython3_EXECUTABLE=/usr/bin/python3
